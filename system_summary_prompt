You are a backend IoT software engineer.

I already have a working system:
- ESP32 Node → LoRa → HQ → PHP + MySQL server
- Sensor data is stored in a MySQL database
- All sensor data available from each hardware-id available

Your task is to ADD a system analytics feature that generates a summary report like this:
- Reporting calculate every 6 hours data available 
=== SYSTEM SUMMARY REPORT ===

Date: YYYY-MM-DD

Total Records: X
Normal Conditions: X (%)
Abnormal Conditions: X (%)
Critical Conditions: X (%)

//depends on sensor available (Dynamic GPIO configuration)
Max Temperature: X°C
Min Temperature: X°C
Average Temperature: X°C

Critical Events:
- Time → Parameter Value

System Status:
(Stable / Moderate instability / Critical instability)

---------------------------------------
REQUIREMENTS
---------------------------------------

1. DATA SOURCE
- Use existing database table (sensor logs)
- Fields include:
  Dynamic sensor available, timestamp, priority

---------------------------------------

2. CLASSIFICATION LOGIC

Classify each record:

NORMAL: (Example)
- Temperature: 24–30°C
- pH: 6–8

ABNORMAL: (Example)
- Slight deviation from normal range

CRITICAL: (Example)
- Temperature > 32°C or < 20°C
- pH < 5 or > 9

---------------------------------------

3. CALCULATIONS

Compute:

- Total number of records
- Count and percentage of:
  - Normal
  - Abnormal
  - Critical

- Temperature:
  - MAX
  - MIN
  - AVERAGE

---------------------------------------

4. CRITICAL EVENTS

- Extract all CRITICAL records
- Show:
  - time (HH:MM)
  - parameter that triggered event
  - value

Example:
10:07 → pH 8.5

---------------------------------------

5. SYSTEM STATUS LOGIC

Based on % of critical:

- 0–2% → Stable
- 3–10% → Moderate instability
- >10% → Critical instability

---------------------------------------

6. OUTPUT FORMAT

Generate EXACTLY this format:

=== SYSTEM SUMMARY REPORT ===

Date: YYYY-MM-DD

Total Records: X
Normal Conditions: X (X%)
Abnormal Conditions: X (X%)
Critical Conditions: X (X%)

(Example)
Max Temperature: X°C
Min Temperature: X°C
Average Temperature: X°C

Critical Events:
- Time → Parameter Value

System Status:
⚠️ Moderate instability detected

---------------------------------------

7. IMPLEMENTATION

Generate:

- PHP script (summary_report.php)
- SQL queries for:
  - aggregation
  - classification
- Clean and efficient code
- Use prepared statements

---------------------------------------

8. CONSTRAINTS

- Do NOT rewrite existing database
- Only ADD this feature
- Keep code modular
- Handle empty data safely

---------------------------------------

GOAL

Provide a real-time analytics summary that helps evaluate system stability and sensor behavior.